# Validation 1.4.2 — variantes simulées Chef d’équipe et Admin Maison

Ce document définit la campagne reproductible du lot intermédiaire **1.4.2** en correction. Il valide l’ajout des variantes simulées **Chef d’équipe** et **Admin Maison** au sélecteur local de `/projet`, la préparation de publication conditionnée par l’état final, le tableau de bord général fictif de `/accueil`, la séparation effective des outils interdits et la non-régression des variantes Sound Designer et Réviseur.

Le lot **1.4.1** reste acquis selon son [protocole historique](1.4-role-variants.md). Le lot 1.4.2 reste **en correction et non acquis jusqu’à sa revalidation humaine**. Cette campagne ne valide ni la sous-étape 1.4 complète, ni la phase 1, ni le démarrage du lot 1.4.3 ; la phase 2 reste non commencée et la sous-étape 2.1 n’est pas la prochaine étape exécutable.

## Livraison à identifier

Avant toute conclusion, consigner :

- la branche testée ;
- le SHA complet du commit ;
- l’URL de la pull request ;
- Google Chrome et les dimensions de viewport utilisées ;
- la date, le système d’exploitation et l’identité du validateur.

Une modification postérieure au SHA consigné impose de rejouer les contrôles concernés.

## Périmètre du lot

Le lot couvre :

- quatre choix de rôle simulé : Sound Designer, Réviseur, Chef d’équipe et Admin Maison ;
- Sound Designer comme état initial après chargement et nouvelle entrée dans `/projet` ;
- une bascule purement locale, non persistante, sans changement d’URL ni ajout dans l’historique du navigateur ;
- un arbre Chef d’équipe centré sur tableau de bord, progression, livre, simulation, historique, commentaires et validation finale ;
- une règle locale et réversible qui ne monte la préparation de publication qu’après la validation finale, jamais dans l’état fictif courant `En attente Chef` ;
- trois axes de progression distincts : Doublage, Révision fondée sur les validations obtenues et attendues, et état textuel de Validation finale ;
- un arbre Admin Maison centré sur membres, équipes, invitations, projets, permissions et audit ;
- un tableau de bord général fictif sur `/accueil`, distinct de l’écran projet, qui présente simultanément toutes les équipes et tous les projets du jeu de démonstration local ;
- l’absence réelle dans le DOM des outils interdits pour chaque rôle ;
- le nettoyage des simulations, historiques et tiroirs lors du démontage de leur workspace ;
- le rendu Chrome à `1440 × 1000` et `768 × 1024` ;
- la navigation clavier, le focus visible, les libellés accessibles, l’unicité du `h1` et l’absence de débordement horizontal.

Le lot n’ajoute aucune authentification, permission réelle, identité de phase 2, persistance, donnée distante, modification audio, gestion effective de membres ou invitations, validation, rejet, publication ou dépublication. Toutes les données de management et toutes les formulations d’action sont fictives, locales et non exécutoires.

## Préconditions

- Node.js 24 ou plus récent ;
- pnpm 11.19.0 via Corepack ;
- Google Chrome disponible pour Playwright ;
- dépôt positionné sur le SHA à valider ;
- dépendances installées depuis le lockfile ;
- aucun service distant requis.

```powershell
corepack pnpm install --frozen-lockfile
```

Le dossier local non versionné `EPUB tests/`, s’il est présent, doit rester hors index et strictement inchangé.

## Contrôles automatiques obligatoires

Depuis la racine du dépôt, sur le SHA à valider :

```powershell
corepack pnpm validate
corepack pnpm test:e2e
$mergeBase = git merge-base HEAD origin/main
git diff --check "$mergeBase..HEAD"
git status --short
```

Résultat attendu : toutes les vérifications TypeScript, ESLint, Vitest et Playwright réussissent ; les deux projets Chrome passent sans erreur console ni erreur page ; le diff ne contient aucune erreur d’espace ; le statut Git ne révèle aucune modification inattendue en dehors du dossier local `EPUB tests/`.

## Contrôles fonctionnels obligatoires

### 1. Sélecteur et état initial

1. Ouvrir directement `/projet`.
2. Vérifier la présence des quatre choix.
3. Vérifier que Sound Designer est le seul choix annoncé actif avec `aria-pressed="true"`.
4. Recharger la page, puis quitter et entrer de nouveau dans `/projet`.

Résultat attendu : Sound Designer redevient actif ; aucun choix n’est persisté.

### 2. Tableau de bord général

1. Ouvrir `/accueil`.
2. Vérifier les régions `Toutes les équipes` et `Tous les projets`.
3. Vérifier que les deux équipes et les cinq projets du jeu local sont tous présents.
4. Vérifier pour chaque équipe le nombre de membres et les projets associés ou leur nombre.
5. Vérifier pour chaque projet le nom, l’équipe, le statut et un repère de progression ou d’activité.
6. Ouvrir le projet de démonstration depuis l’accueil.

Résultat attendu : l’accueil reste un tableau de bord général fictif, local et distinct du projet ; la navigation est une transition SPA vers `/projet` et Sound Designer y est actif initialement.

### 3. Variante Chef d’équipe

Activer Chef d’équipe et vérifier les régions suivantes :

- Tableau de bord ;
- Progression ;
- Livre ;
- Simulation/Navigation ;
- Historique ;
- Commentaires ;
- Validation finale.

Résultat attendu : le contexte d’en-tête annonce Chef d’équipe, un seul `h1` est présent, le livre et la simulation restent consultables et les décisions affichées sont explicitement fictives ou non exécutoires. Dans l’état `En attente Chef`, la région `Préparation de la publication`, ses contrôles et ses métadonnées de destination sont absents du DOM ; le panneau Validation finale explique qu’ils deviendront disponibles après validation.

### 4. Progression séparée

Dans la région Progression, vérifier séparément :

- Doublage avec chapitres terminés et total ;
- Révision avec validations obtenues et attendues ;
- Validation finale avec un état textuel prévu par le produit.

Résultat attendu : aucun pourcentage global unique ne remplace ces trois axes.

### 5. Interdictions Chef d’équipe

Inspecter le DOM et l’arbre d’accessibilité.

Résultat attendu : aucune bibliothèque audio, aucun sélecteur de fichier local, aucun inspecteur audio, aucune commande de montage, aucune préparation de publication avant validation finale et aucune action de dépublication ne sont montés. Jaquette désigne l’outil de production et Jacques la destination de publication.

### 6. Variante Admin Maison

Activer Admin Maison et vérifier les régions suivantes :

- Membres ;
- Équipes ;
- Invitations ;
- Projets ;
- Permissions ;
- Audit.

Résultat attendu : les données sont manifestement fictives et locales ; les aperçus n’effectuent aucune gestion réelle.

### 7. Interdictions Admin Maison

Inspecter le DOM et l’arbre d’accessibilité.

Résultat attendu : aucune bibliothèque audio, aucun sélecteur de fichier local, aucun inspecteur audio, aucune commande de montage, aucun outil de validation Réviseur, aucune validation finale et aucune préparation de publication Chef d’équipe ne sont montés.

### 8. Bascule, navigation et nettoyage

1. Démarrer une simulation et ouvrir l’historique dans un workspace qui les propose.
2. Parcourir les quatre rôles deux fois.
3. Observer le focus, l’URL, la longueur de l’historique du navigateur et un marqueur JavaScript conservé en mémoire.

Résultat attendu : un seul arbre de rôle est monté à la fois ; simulation, historique et tiroirs démontés ne fuient pas ; le focus reste visible ; l’URL, l’historique et le marqueur ne changent pas.

### 9. Responsive et accessibilité

Aux formats `1440 × 1000` et `768 × 1024` :

- sur `/accueil`, faire défiler et consulter toutes les équipes et tous les projets ;
- vérifier l’ordre logique des régions `Toutes les équipes` puis `Tous les projets` ;
- vérifier la lisibilité des quatre choix de rôle ;
- vérifier l’ordre logique des régions ;
- faire défiler les workspaces pour atteindre chaque région ;
- vérifier l’absence de libellé tronqué, chevauchement ou débordement horizontal ;
- parcourir le sélecteur au clavier et activer un rôle avec `Espace` ;
- vérifier qu’un seul `h1` est présent.

## Captures de référence à relire

La suite Playwright régénère dans `test-results/visual/` :

| Rôle | Viewport | Capture |
|---|---:|---|
| Accueil général | `1440 × 1000` | `accueil-dashboard-1440x1000.png` |
| Accueil général | `768 × 1024` | `accueil-dashboard-768x1024.png` |
| Chef d’équipe | `1440 × 1000` | `projet-team-lead-1440x1000.png` |
| Chef d’équipe | `768 × 1024` | `projet-team-lead-768x1024.png` |
| Admin Maison | `1440 × 1000` | `projet-admin-maison-1440x1000.png` |
| Admin Maison | `768 × 1024` | `projet-admin-maison-768x1024.png` |

Relire les six captures avant partage. Vérifier les alignements, l’espacement, le contraste, la lisibilité du sélecteur, la distinction textuelle des états, l’absence de contenu coupé et la cohérence de la hiérarchie. Ces fichiers sont des artefacts locaux de test et non des données produit.

## Checklist manuelle à consigner

1. Les quatre choix sont lisibles et Sound Designer est actif initialement — `PASS` / `FAIL`.
2. L’accueil présente toutes les équipes et tous les projets du jeu fictif, puis ouvre `/projet` en SPA avec Sound Designer actif — `PASS` / `FAIL`.
3. La variante Chef d’équipe présente toutes ses régions et trois axes de progression distincts — `PASS` / `FAIL`.
4. Dans l’état `En attente Chef`, la préparation de publication est absente et son indisponibilité avant validation est expliquée — `PASS` / `FAIL`.
5. Aucun outil de montage ou de dépublication n’est accessible au Chef d’équipe — `PASS` / `FAIL`.
6. La variante Admin Maison présente membres, équipes, invitations, projets, permissions et audit — `PASS` / `FAIL`.
7. Aucun droit métier éditorial implicite ni outil interdit n’est accessible à l’Admin Maison — `PASS` / `FAIL`.
8. Les bascules répétées préservent focus, URL et historique tout en nettoyant les états transitoires — `PASS` / `FAIL`.
9. Les formats `1440 × 1000` et `768 × 1024` sont lisibles, accessibles et sans débordement — `PASS` / `FAIL`.
10. Les six captures correspondent au SHA testé et ont été relues — `PASS` / `FAIL`.

## Conclusion

Reporter les résultats dans [`VALIDATION_REPORT.md`](VALIDATION_REPORT.md) et appliquer les règles du [`README`](README.md) de ce dossier. Après réussite de la campagne corrective, et tant que la nouvelle décision humaine n’est pas rendue, le statut du lot reste **PRÊT POUR REVALIDATION HUMAINE**. Même après validation du lot 1.4.2, ne pas déclarer la sous-étape 1.4 ou la phase 1 acquise : le lot 1.4.3 et la clôture de phase restent hors de cette campagne.
