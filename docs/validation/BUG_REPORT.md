# Rapport de bug — modèle

## Identification

- Titre : `<résumé précis du défaut>`
- Contrôle concerné : `<ID de checklist>`
- Projet/sous-étape : `<valeur>`
- Commit testé : `<SHA complet>`
- Branche : `<nom>`
- Pull request : `<URL ou numéro>`
- Auteur du rapport : `<nom ou identifiant>`
- Date et heure : `<date, heure et fuseau>`

## Contexte et environnement

- Système et version : `<valeur>`
- Navigateur ou application et version : `<valeur>`
- Matériel pertinent : `<valeur ou non applicable>`
- Données de test et configuration non sensible : `<valeur>`
- Préconditions : `<état nécessaire avant reproduction>`

## Reproduction

1. `<première étape exacte>`
2. `<étape suivante>`
3. `<étape qui révèle le défaut>`

## Résultats

- Résultat attendu : `<comportement observable prévu>`
- Résultat réellement observé : `<comportement exact, message et conséquences>`
- Fréquence : `<systématique ou nombre de reproductions sur nombre de tentatives>`
- Gravité : `<bloquante/haute/normale/faible>`
- Justification de la gravité : `<impact utilisateur, données, sécurité ou workflow>`

## Éléments associés

- Preuves : `<captures, journaux, vidéo ou référence contrôlée>`
- Régressions associées : `<comportements auparavant fonctionnels ou aucune connue>`
- Contournement connu : `<procédure ou aucun>`
- Informations complémentaires : `<valeur ou aucune>`

## Confidentialité des pièces jointes

- [ ] Les pièces jointes ont été relues.
- [ ] Elles ne contiennent aucun secret, jeton, mot de passe ou identifiant sensible.
- [ ] Elles ne contiennent aucune donnée privée, aucun manuscrit ni contenu confidentiel non autorisé.
- [ ] Toute information sensible utile a été masquée ou remplacée par une description partageable.
